/*#define BOTON "boton.jpg"

SDL_Surface *pantalla;
void CrearBoton(int x, int y, const char *file, int tam_label, int color)
{
    SDL_Surface *boton, *img_label;
    SDL_Rect *pos_boton, *pos_label;
    TTF_Font *label;
    SDL_Color paleta_color;
    boton = IMG_Load(BOTON);                     // posicion boton
    pos_boton = (SDL_Rect){x, y, 0, 0};          // posicion etiqueta
    pos_label = (SDL_Rect){x + 5, y + 15, 0, 0}; // cargar fuente y tamano
}*/